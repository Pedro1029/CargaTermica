import { FastifyInstance } from "fastify";
import { prisma } from "../lib/prisma";

export async function questionarioRoutes(app: FastifyInstance) {
    app.get('/questionariocargatermica', async () => {

    })

    app.post('/questionariocargatermica', async () => {

    })

    app.put('/questionariocargatermica', async () => {

    })

    app.delete('/questionariocargatermica', async () => {

    })
}